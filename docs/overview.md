# Overview

DSP v2 compiles repository structure into a graph used by humans and AI coding agents.

Main output:

- deterministic entities and relations
- provenance and confidence on every assertion
- incremental graph updates
- context packs bounded by token budgets

MVP languages:

- TypeScript/JavaScript
- Python
- Rust
- Ruby
